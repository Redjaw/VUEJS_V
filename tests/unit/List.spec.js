import { mount } from '@vue/test-utils'
import ListComponent from '@/components/List.vue'

describe('La pagina lista', () => {
    it.todo('deve iniziare caricando il servizio gamelist')
    it.todo('deve essere popolata con il numero di elementi ricevuti dal servizio')
    it.todo('deve mostrare un errore se il servizio fallisce')
})